# requirements.txt
# Dipendenze Python per Vulnerability Assessment Platform

# === WEB FRAMEWORK ===
fastapi==0.109.0
uvicorn[standard]==0.27.0
jinja2==3.1.3
python-multipart==0.0.6

# === DATABASE ===
sqlalchemy==2.0.25
alembic==1.14.1

# === PDF GENERATION ===
reportlab==4.0.9

# === SECURITY TOOLS ===
requests==2.32.3
chardet>=3.0.2,<6
slowapi==0.1.9
# SECURITY NOTE: passlib is in maintenance-only mode. Consider migrating to
# `pwdlib` or `argon2-cffi` directly for future projects.
passlib[bcrypt]==1.7.4
argon2-cffi==23.1.0
# SECURITY NOTE: python-jose 3.3.0 is affected by CVE-2022-29217 (algorithm
# confusion). Consider replacing with `PyJWT>=2.8` or `joserfc` if RS256/ES256
# support is needed. For HS256-only usage the practical risk is low, but
# upgrading to a maintained library is recommended.
python-jose==3.3.0
structlog==24.1.0
# SECURITY NOTE: bleach 6.1.0 is the final release; the project is no longer
# maintained. It is currently used only for stripping HTML from scan targets
# (scanner_engine.py). For new HTML sanitisation needs, consider `nh3` or
# `lxml.html.clean`.
bleach==6.1.0
validators==0.22.0
itsdangerous==2.1.2
# NOTE: SQLCipher support (optional) requires pysqlcipher3-binary.
# Install separately if VAP_SQLCIPHER_KEY is set in production.

# === DATA PROCESSING ===
python-dateutil==2.8.2

# === UTILITIES ===
pydantic==2.5.3
python-dotenv==1.0.1
celery==5.3.6
redis==5.0.1
apscheduler==3.10.4
prometheus-client==0.20.0

# === TESTING (opzionale) ===
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
pytest-cov==4.1.0
