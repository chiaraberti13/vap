╔══════════════════════════════════════════════════════════════╗
║    VULNERABILITY ASSESSMENT PLATFORM - COMPLETE PACKAGE     ║
╚══════════════════════════════════════════════════════════════╝

📦 PACKAGE CONTENTS:

✅ Configuration files:
   - installer.sh (Linux/macOS installer)
   - installer.ps1 (Windows installer)
   - requirements.txt (Python dependencies)
   - .env.example (configuration)
   - .gitignore

✅ Documentation:
   - README.md (full guide)
   - INSTALLATION_GUIDE.md (step-by-step installation)

✅ Directory structure:
   - scanners/ (scanner modules)
   - templates/ (HTML templates)
   - static/ (CSS/JS)
   - reports/ (PDF output)
   - logs/ (application logs)
   - scans/ (scan data)

🔧 INSTALLATION:

1. Extract the archive:
   tar -xzf vulnerability-assessment-platform.tar.gz
   cd vulnerability-assessment-platform

2. Ensure all required files are present:
   The installer will validate required files and stop if anything is missing.

3. Run the installer:
   chmod +x installer.sh
   ./installer.sh

4. Start the platform:
   source venv/bin/activate
   python3 app.py

🌐 ACCESS:
   http://localhost:8000

⚠️  IMPORTANT NOTE:
   This package includes the full structure and configuration files.
   If any Python or HTML files are missing or empty, restore them from
   your source archive before running the installer.

📧 SUPPORT:
   For support, consult your project documentation or internal team channels.
